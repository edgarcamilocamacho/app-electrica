/// <reference types="vite/client" />

/** Identificador del build, inyectado por Vite (vite.config.ts). */
declare const __BUILD_ID__: string;

/** Versión de la app (semver), la de package.json. */
declare const __APP_VERSION__: string;
